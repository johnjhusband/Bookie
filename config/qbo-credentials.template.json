{
  "_comment": "QuickBooks Online OAuth credentials. Copy to ~/.config/bookie/qbo-credentials.json and fill in. Get the client_id+secret from developer.intuit.com (your app's keys). refresh_token + realm_id come from completing the OAuth flow — run `scripts/qbo-authorize.sh` to do that interactively.",
  "client_id": "",
  "client_secret": "",
  "refresh_token": "",
  "realm_id": "",
  "environment": "sandbox",
  "access_token": "",
  "access_token_expires_at": 0
}
