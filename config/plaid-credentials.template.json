{
  "_comment": "Plaid API credentials. Copy to ~/.config/bookie/plaid-credentials.json and fill in. Get client_id + SANDBOX secret from dashboard.plaid.com > Team Settings > Keys (https://dashboard.plaid.com/developers/keys). Sandbox and Production have separate secrets — start with Sandbox. The 'development' environment was retired 2024-06-20; only sandbox + production are valid.",
  "client_id": "",
  "secret": "",
  "environment": "sandbox"
}
